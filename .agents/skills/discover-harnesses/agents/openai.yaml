interface:
  display_name: "Continue in The Build Bench"
  short_description: "Route legacy discovery to the canonical directory"
  default_prompt: "Use $discover-harnesses to hand this legacy catalog request to The Build Bench's canonical checkout and discovery skill."
