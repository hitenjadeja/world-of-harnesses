---
name: discover-harnesses
description: Route legacy World of Harnesses discovery and catalog requests to the canonical The Build Bench repository. Use when invoked from this legacy checkout; do not run a separate discovery or publishing workflow here.
---

# Continue discovery in The Build Bench

This checkout is a retired site and frozen historical catalog. Its prepared redirect is retained only for explicitly requested recovery and is not deployed. Do not run its discovery or refresh scripts, add records to `data/additions.json`, or publish another catalog here.

Use `/Users/hiten/workspace/the-build-bench`, whose origin is `https://github.com/hitenjadeja/the-build-bench.git`. Read that checkout's `AGENTS.md` and `.agents/skills/discover-harnesses/SKILL.md`, then follow its canonical procedure and `data/harnesses.v1.json` schema within the user's authorized scope.

If the canonical checkout is unavailable, identify that concrete limitation and establish it when authorized. Do not treat the legacy checkout as a substitute or change its remote to the canonical repository.

Historical data and provenance remain in this repository; ongoing candidate review and catalog maintenance belong to The Build Bench.
